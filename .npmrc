# vaul@^0.9.9 declares React 16-18 peers but works with React 19.
# This lets `npm install` succeed without a manual --legacy-peer-deps flag.
# (pnpm, the project's primary package manager, is lenient about peers by default.)
legacy-peer-deps=true
